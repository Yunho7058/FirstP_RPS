// import React from 'react';

// const Btn = ({ choice, play }) => {
//   return (
//     <div>
//       {choice.map((el) => (
//         <button key={el.id} onClick={() => play(el.name)}>
//           {el.name}
//         </button>
//       ))}
//     </div>
//   );
// };

// export default Btn;
